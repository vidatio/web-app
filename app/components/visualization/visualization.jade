.visualization

    leaflet(id="map" geojson="geojson" ng-show="options.type == 'map'")

    #chart(ng-hide="options.type == 'map' || !options.type")

    .placeholder(ng-show="!options.type")
        img(src="images/logo-greyscale.svg")
