# Share Controller
# ===================

"use strict"

app = angular.module "app.controllers"

app.controller "ShareCtrl", [
    "$scope"
    "$translate"
    "$rootScope"
    "DataService"
    ($scope, $translate, $rootScope, Data) ->

]
