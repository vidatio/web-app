#table
    font-size 14px

    #datatable
        width 100%
        height calc(100% - 50px) /* -50px according to the settings-bars' height + 20px margin */
        overflow hidden

        // Bugfix for scrollbar
        // TODO: Further check later
        .ht_master .wtHolder
            width 100% !important

        // Column headers (sticky and non-sticky)
        .ht_master thead,
        .ht_clone_top thead,
        .ht_clone_left thead,
        .ht_clone_left tbody,
        .ht_clone_corner thead
            tr
                th
                    background $color-hover-light
                    border 2px solid white

                &:nth-child(odd) th
                    &.current-col
                    &.current-row
                        background $color-green !important
                &:nth-child(even) th
                    &.current-col
                    &.current-row
                        background $color-green !important
        // Body
        .ht_master
            th, td
                border 2px solid $color-white

            tbody tr
                &:nth-child(odd) td
                    background $color-table-odd-passive
                    &.current-col
                    &.current-row
                        background $color-table-odd-active !important

                &:nth-child(even) td
                    background $color-table-even-passive
                    &.current-col
                    &.current-row
                        background $color-table-even-active !important

            tbody tr td.highlight
                background $color-hover-light !important

        // Input field
        .handsontableInputHolder
            padding 2px
            textarea
                background $color-grey-light

        .wtBorder
            &.current
                background-color $color-green !important
